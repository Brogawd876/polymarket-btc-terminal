# Dependency Map

- `@fastify/*`, `better-sqlite3`, `ethers`, `dotenv` -> `apps/server` (Currently `^` versioned)
- `wxt`, `react`, `tailwindcss` -> `apps/extension` (Currently `^` versioned)
- `zod` -> `packages/shared`, Root (Currently `^` versioned)
