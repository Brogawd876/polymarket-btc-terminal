# Dependency Map

- **Fastify WebSocket** (`@fastify/websocket`): Used in server for bridging the RTDS stream to connected clients.
- **External Websockets**: Polymarket's RTDS Stream for Chainlink data (external dependency).
- **Zod**: Used in `@polymarket-btc/shared` to validate WebSocket message schemas.
